# ─── PDF Processing ───────────────────────────────────────────────────────────
pymupdf                   # fitz: fast PDF text extraction (Python 3.13 compatible)
pdfplumber                # layout-aware PDF parsing (tables, columns)

# ─── OCR & LLM Engine ──────────────────────────────────────────────────────────
pytesseract               # Python wrapper for Tesseract OCR
google-genai              # Official Google Gemini SDK for LLM extraction
pydantic                  # Strict JSON schema validation for LLM output

# ─── Image Processing ─────────────────────────────────────────────────────────
opencv-python             # image preprocessing (deskew, threshold, denoise)
Pillow                    # image I/O and format conversions

# ─── Data Cleaning & Processing ───────────────────────────────────────────────
pandas                    # tabular data manipulation
numpy                     # numerical operations (used by OpenCV/Pandas)

# ─── Excel Export ─────────────────────────────────────────────────────────────
openpyxl                  # read/write .xlsx files
XlsxWriter                # advanced Excel formatting & charts

# ─── Web UI (Week 3) ────────────────────────────────────────────────────────────
streamlit                 # web interface for the pipeline

# ─── Utilities ────────────────────────────────────────────────────────────────
python-dotenv             # load .env config (e.g. Tesseract path)
loguru                    # structured logging
tqdm                      # progress bars for batch processing
